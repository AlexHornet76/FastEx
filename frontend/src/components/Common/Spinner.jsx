import './Common.css'

/**
 * Spinner - displays while loading
 * Used in buttons and during API calls
 */
export default function Spinner() {
  return <div className="spinner" aria-label="Loading"></div>
}